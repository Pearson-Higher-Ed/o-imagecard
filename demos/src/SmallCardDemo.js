"use strict";

var ImageCard = require("../../main.js");


// From the README to make sure the example works
var json = { "src" : "../src/small.jpg", "size": "small" };
new ImageCard("#cardDiv", json);
